export interface Flashcard {
  id: number;
  title: string;
  description: string;
  language: string;
  iconColor: string;
}
export interface FlashcardParams {
  title: string;
  description: string;
  language: string;
  iconColor: string;
}
